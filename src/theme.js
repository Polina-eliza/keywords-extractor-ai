import { extendTheme } from "@chakra-ui/react";
import SpaceGrotesk from "@fontsource/space-grotesk";

const theme = extendTheme({
  fonts: {
    body: "SpaceGrotesk",
    heading: "SpaceGrotesk",
  },
});

export default theme;